let btn = document.getElementById(btn);
const element = [];
let clock = document.getElementById(clock).value;

btn.addEventListener("click" , function(){
    for(element;i=0;i++){
        element[i] = document.getElementById(i+1)
        clock.innerHTML = 5--;
    }
})